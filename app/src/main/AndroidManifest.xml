<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 인터넷 권한 (서버 통신용) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".auth.GlobalApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_meokplaylist_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_meokplaylist_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Meokpli"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="31">

        <!-- ✅ 카카오 리다이렉트 수신 -->
        <activity
            android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="kakao1a0fd1421e84e625979ad2a917b4e262"
                    android:host="oauth"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".auth.LoginActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 나머지 Activity 목록 -->
        <activity android:name=".auth.SignUpActivity" />
        <activity android:name=".main.MainActivity"/>
        <activity android:name=".user.InitProfileActivity" />
        <activity android:name=".user.CategoryActivity" />
        <activity android:name=".user.ConsentFormActivity" />
        <activity android:name=".user.ForgotPasswordActivity" />
        <activity android:name=".user.ResetPasswordActivity" />
        <activity android:name=".user.Region.RegionActivity" />
        <activity android:name=".user.WelcomeActivity"/>
        <activity
            android:name=".user.EditProfileActivity"/>
        <activity android:name=".user.PasswordForgotActivity" />

        <!-- 누락된 액티비티들 추가 -->
        <activity android:name=".user.ChangePasswordActivity" />
        <activity android:name=".user.TermsActivity" />
        <activity android:name=".user.AccountCenterActivity" />
        <activity android:name=".user.AccountInfoActivity" />
        <activity
            android:name=".user.OnboardingActivity"
            android:exported="false" />
        <activity
            android:name=".main.Home.FeedDetailActivity"
            android:exported="false" />


        <!-- 사전 로딩할 폰트 설정 -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

    <!-- Android 10+에서 사진의 위치 메타데이터 접근 -->
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

</manifest>