<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 인터넷 권한 (서버 통신용) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Auth.GlobalApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Meokpli"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="31">

        <!-- ✅ 카카오 리다이렉트 수신 -->
        <activity
            android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="kakao1a0fd1421e84e625979ad2a917b4e262"
                    android:host="oauth"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".Auth.LoginActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 나머지 Activity 목록 -->
        <activity android:name=".Auth.SignUpActivity" />
        <activity android:name=".Main.MainActivity" />
        <activity android:name=".User.InitProfileActivity" />
        <activity android:name=".User.CategoryActivity" />
        <activity android:name=".User.ConsentFormActivity" />
        <activity android:name=".User.ForgotPasswordActivity" />
        <activity android:name=".User.ResetPasswordActivity" />
        <activity android:name=".User.Region.RegionActivity" />
        <activity android:name=".User.WelcomeActivity"/>
        <activity
            android:name=".gallery.GalleryBottomSheet"
            android:exported="false" />


        <!-- 사전 로딩할 폰트 설정 -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

    <!-- Android 10+에서 사진의 위치 메타데이터 접근 -->
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

</manifest>