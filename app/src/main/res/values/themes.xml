<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Meokpli" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="materialAlertDialogTheme">
            @style/ThemeOverlay.MaterialComponents.MaterialAlertDialog
        </item>

        <!-- Primary brand color -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/black</item>
        <item name="colorSecondaryVariant">@color/black</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- ✅ 상태바 다시 보이게 (딱 한 줄 추가) -->
        <item name="android:windowFullscreen">false</item>

        <!-- 상태바 색상 -->
        <item name="android:statusBarColor">@android:color/black</item>

        <item name="checkboxStyle">@style/CustomCheckBox</item>
    </style>

    <style name="CustomCheckBox" parent="Widget.AppCompat.CompoundButton.CheckBox">
        <item name="android:button">@null</item>
        <item name="buttonCompat">@drawable/checkbox_circle_fill</item>
    </style>

    <style name="AppBottomSheetDialogTheme" parent="ThemeOverlay.MaterialComponents.BottomSheetDialog" />
</resources>
