<?xml version="1.0" encoding="utf-8"?>
<!-- res/layout/bottom_sheet_feed_actions.xml -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="20dp"
    android:paddingTop="10dp"
    android:paddingBottom="24dp"
    android:background="@drawable/bg_sheet_round_top"
    android:clipToPadding="false">

    <!-- 핸들 -->
    <View
        android:layout_width="36dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:background="@drawable/bg_sheet_handle"
        android:layout_marginTop="6dp"
        android:layout_marginBottom="10dp"/>

    <!-- 1. 게시글 수정 -->
    <LinearLayout
        android:id="@+id/rowEditPost"
        style="@style/ActionRowStyle"
        android:paddingVertical="14dp"
        android:foreground="?attr/selectableItemBackground"
        android:clickable="true"
        android:focusable="true">
        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_pen"/>
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:text="게시글 수정"
            android:textSize="16sp"
            android:textColor="#111111"
            android:fontFamily="@font/inter_medium"/>
    </LinearLayout>

    <View style="@style/DividerStyle"/>

    <!-- 2. 대표사진 수정 -->
    <LinearLayout
        android:id="@+id/rowEditCover"
        style="@style/ActionRowStyle"
        android:paddingVertical="14dp"
        android:foreground="?attr/selectableItemBackground"
        android:clickable="true"
        android:focusable="true">
        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_picture"/>
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:text="대표사진 수정"
            android:textSize="16sp"
            android:textColor="#111111"
            android:fontFamily="@font/inter_medium"/>
    </LinearLayout>

    <View style="@style/DividerStyle"/>

    <!-- 3. 게시글 카테고리 수정 -->
    <LinearLayout
        android:id="@+id/rowEditCategory"
        style="@style/ActionRowStyle"
        android:paddingVertical="14dp"
        android:foreground="?attr/selectableItemBackground"
        android:clickable="true"
        android:focusable="true">
        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_category"/>
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:text="게시글 카테고리 수정"
            android:textSize="16sp"
            android:textColor="#111111"
            android:fontFamily="@font/inter_medium"/>
    </LinearLayout>

    <View style="@style/DividerStyle" android:background="#EEEEEE"/>

    <!-- 4. 삭제 (빨강) -->
    <LinearLayout
        android:id="@+id/rowDelete"
        style="@style/ActionRowStyle"
        android:paddingVertical="14dp"
        android:foreground="?attr/selectableItemBackground"
        android:clickable="true"
        android:focusable="true">
        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_trashcan"/>
        <TextView
            android:id="@+id/tvDelete"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:text="삭제"
            android:textSize="16sp"
            android:textColor="#C64132"
            android:fontFamily="@font/inter_medium"/>
    </LinearLayout>

</LinearLayout>
